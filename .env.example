# Copy this file to ".env" and fill in your key to enable real AI strategy.
# Without it, the app still runs fully using its built-in local matching engine.

ANTHROPIC_API_KEY=
# Optional overrides:
# ANTHROPIC_MODEL=claude-sonnet-4-6
# PORT=8787
